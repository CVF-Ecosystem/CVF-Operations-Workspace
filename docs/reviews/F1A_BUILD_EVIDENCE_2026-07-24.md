# F1A BUILD Evidence — Versioned Contract Foundation

- Implements: `ADR-OW-005`, `OW-F1A-SPEC-001`, `OW-F1A-WO-001`
- Role: `IMPLEMENTATION_WORKER` (Claude, provider-neutral role contract)
- Date: 2026-07-24
- Status: **self-reported BUILD complete, not yet independently reviewed, not
  REVIEW_PASS'd, not FREEZE'd**

## Pre-BUILD verification (reproduced live this round)

- Target `CVF-Operations-Workspace`: HEAD = `origin/main` =
  `d731762a9e135b075261831ed7eb0df4badc98dd`, worktree clean before BUILD.
  This is `OW-F1A-WO-001`'s independently REVIEW_PASS'd, committed,
  rehearsed and pushed C1.
- CVF core: HEAD = `27137db4d9aa2aea931ddd2507185d5c24943080`, matching
  `.cvf/manifest.json`, worktree clean.
- Read-only inputs unchanged: `shift-operations-workspace` worktree clean
  except the pre-existing untracked
  `docs/decisions/ASSESSMENT_2026-07-23_OPERATIONS_WORKSPACE_REPOSITIONING.md`;
  `operations-workspace-all-phases` not re-touched.
- `docs/catalog/MODULE_REGISTRY.json` confirmed empty (`modules: []`) before
  this BUILD added its one entry.
- `ADR-OW-005`, `OW-F1A-SPEC-001`, `OW-F1A-WO-001` read in full at this
  commit.

## Exact BUILD changed set

New paths:

- `contracts/core/common-definitions.schema.json`
- `contracts/core/profile-manifest.schema.json`
- `contracts/core/operational-session.schema.json`
- `contracts/core/command-envelope.schema.json`
- `contracts/core/event-envelope.schema.json`
- `contracts/core/capability-manifest.schema.json`
- `requirements-contract-validator.txt`
- `tests/contracts/__init__.py`
- `tests/contracts/test_f1a_contracts.py`
- `docs/reviews/F1A_BUILD_EVIDENCE_2026-07-24.md` (this file)

Modified paths:

- `docs/catalog/MODULE_REGISTRY.json` (one entry added, exact authorized
  content)
- `docs/catalog/MODULE_CATALOG.md` (generated only, via
  `scripts/manage_cvf_downstream_catalog.ps1 -Write`)

Continuity paths updated separately (see "Continuity" below):
`IMPLEMENTATION_STATUS.json`, `CVF_SESSION/ACTIVE_SESSION_STATE.json`,
`CVF_SESSION/handoffs/AGENT_HANDOFF_V1_2026-07-23.md`.

`docs/INDEX.md` was regenerated by the same `-Write` invocation (the Golden
manager always rewrites both generated views together) but is
**byte-identical before and after** — `git diff --stat docs/INDEX.md`
produced no output, confirmed. `docs/catalog/ARTIFACT_REGISTRY.json` was not
modified at all (absent from `git status --porcelain`). No other path —
`docs/roadmaps/**`, `.cvf/**`, `AGENTS.md`, any API route, any provider
adapter, either read-only input — was touched.

## Validator dependency evidence (`ADR-OW-005` Decision 12, `F1A-R1`)

Installed with `pip install --require-hashes -r requirements-contract-validator.txt`
into a temporary, isolated virtual environment created solely for this
BUILD's test run (created at a short filesystem path to avoid a Windows
`MAX_PATH` failure seen when a venv was first attempted inside the deeply
nested session scratch directory; the venv is outside this repository's
working tree and is not a committed artifact). Real install output:

```text
Collecting fqdn==1.5.1 (from -r requirements-contract-validator.txt (line 18))
  Using cached fqdn-1.5.1-py3-none-any.whl (9.1 kB)
Collecting idna==3.18 (from -r requirements-contract-validator.txt (line 19))
  Using cached idna-3.18-py3-none-any.whl (65 kB)
Collecting isoduration==20.11.0 (from -r requirements-contract-validator.txt (line 20))
  Using cached isoduration-20.11.0-py3-none-any.whl (11 kB)
Collecting jsonpointer==3.1.1 (from -r requirements-contract-validator.txt (line 21))
  Using cached jsonpointer-3.1.1-py3-none-any.whl (7.7 kB)
Collecting jsonschema==4.26.0 (from -r requirements-contract-validator.txt (line 22))
  Using cached jsonschema-4.26.0-py3-none-any.whl (90 kB)
Collecting jsonschema-specifications==2025.9.1 (from -r requirements-contract-validator.txt (line 23))
  Using cached jsonschema_specifications-2025.9.1-py3-none-any.whl (18 kB)
Collecting lark==1.3.1 (from -r requirements-contract-validator.txt (line 24))
  Using cached lark-1.3.1-py3-none-any.whl (113 kB)
Collecting python-dateutil==2.9.0.post0 (from -r requirements-contract-validator.txt (line 25))
  Using cached python_dateutil-2.9.0.post0-py2.py3-none-any.whl (229 kB)
Collecting referencing==0.37.0 (from -r requirements-contract-validator.txt (line 26))
  Using cached referencing-0.37.0-py3-none-any.whl (26 kB)
Collecting rfc3339-validator==0.1.4 (from -r requirements-contract-validator.txt (line 27))
  Using cached rfc3339_validator-0.1.4-py2.py3-none-any.whl (3.5 kB)
Collecting rfc3986-validator==0.1.1 (from -r requirements-contract-validator.txt (line 28))
  Using cached rfc3986_validator-0.1.1-py2.py3-none-any.whl (4.2 kB)
Collecting rfc3987-syntax==1.1.0 (from -r requirements-contract-validator.txt (line 29))
  Using cached rfc3987_syntax-1.1.0-py3-none-any.whl (8.0 kB)
Collecting rpds-py==2026.6.3 (from -r requirements-contract-validator.txt (line 30))
  Using cached rpds_py-2026.6.3-cp313-cp313-win_amd64.whl (219 kB)
Collecting six==1.17.0 (from -r requirements-contract-validator.txt (line 31))
  Using cached six-1.17.0-py2.py3-none-any.whl (11 kB)
Collecting tzdata==2026.3 (from -r requirements-contract-validator.txt (line 32))
  Using cached tzdata-2026.3-py2.py3-none-any.whl (348 kB)
Collecting uri-template==1.3.0 (from -r requirements-contract-validator.txt (line 33))
  Using cached uri_template-1.3.0-py3-none-any.whl (11 kB)
Collecting webcolors==25.10.0 (from -r requirements-contract-validator.txt (line 34))
  Using cached webcolors-25.10.0-py3-none-any.whl (14 kB)
Installing collected packages: webcolors, uri-template, tzdata, six, rpds-py,
rfc3986-validator, lark, jsonpointer, idna, fqdn, attrs, rfc3987-syntax,
rfc3339-validator, referencing, python-dateutil, jsonschema-specifications,
arrow, jsonschema, isoduration
Successfully installed arrow-1.4.0 attrs-26.1.0 fqdn-1.5.1 idna-3.18
isoduration-20.11.0 jsonpointer-3.1.1 jsonschema-4.26.0
jsonschema-specifications-2025.9.1 lark-1.3.1 python-dateutil-2.9.0.post0
referencing-0.37.0 rfc3339-validator-0.1.4 rfc3986-validator-0.1.1
rfc3987-syntax-1.1.0 rpds-py-2026.6.3 six-1.17.0 tzdata-2026.3
uri-template-1.3.0 webcolors-25.10.0
```

All 19 hash checks passed (`--require-hashes` fails closed on any mismatch;
no such failure occurred). `pip freeze` inside the venv afterward lists
exactly these 19 packages/versions, matching `ADR-OW-005` Decision 12's
table exactly — no extra, no missing, no different version. No source
build occurred for any package (all 19 lines above show a cached/downloaded
`.whl`, never a `Building wheel for ...` line). No `--no-deps` flag was
used. No package was installed into the user/global Python environment —
`pip freeze` in the ambient authoring environment (used for the
pre-existing 116 tests and the catalog/doctor checks) does not include
`jsonschema` at all, confirmed unpolluted.

Live confirmation of `F1A-R3`'s policy inside this exact installed
environment:

```text
>>> import jsonschema
>>> jsonschema.FormatChecker().checkers.__contains__('date-time')
True
>>> v = jsonschema.Draft202012Validator({'type': 'string', 'format': 'date-time'}, format_checker=jsonschema.FormatChecker())
>>> [i for i in ['2026-07-24T10:00:00Z','2026-13-01T00:00:00Z','2026-02-30T00:00:00Z','2026-07-24T25:00:00Z'] if list(v.iter_errors(i))]
['2026-13-01T00:00:00Z', '2026-02-30T00:00:00Z', '2026-07-24T25:00:00Z']
```

The venv is disposable, per `ADR-OW-005` Decision 12; it is not a committed
repository artifact and does not appear in `git status`.

## Acceptance criteria matrix (`OW-F1A-SPEC-001` `F1A-AC-01`–`F1A-AC-31`)

| AC | Result | Evidence |
|---|---|---|
| AC-01 | PASS | `test_exact_files_exist`, `test_exact_ids`, `test_no_other_schema_file_under_contracts` — six files, six `$id`s, no seventh schema file under `contracts/` |
| AC-02 | PASS | `test_dialect_is_2020_12` — all six files `$schema: https://json-schema.org/draft/2020-12/schema` |
| AC-03 | PASS | `test_all_files_are_valid_json`, `test_common_definitions_is_defs_only`, `test_contract_owned_schemas_are_type_object` |
| AC-04 | PASS | `test_each_schema_validates_against_meta_schema` via `jsonschema.Draft202012Validator.check_schema`, using the pinned library's bundled offline meta-schema |
| AC-05 | PASS | `test_no_http_ref_anywhere`, `test_no_http_substring_in_raw_text_ref_context`, `test_all_refs_resolve_in_offline_registry` — every `$ref` is a local `urn:` reference; registry built from the six local files only |
| AC-06 | PASS | `test_positive_fixtures_validate` — one fully-populated fixture per contract-owned schema (5), all valid |
| AC-07 | PASS | `test_missing_required_field_rejected` — one required field dropped per schema, each rejected |
| AC-08 | PASS | `test_unknown_top_level_property_rejected` (5 schemas), `test_unknown_property_inside_ownership_rejected` |
| AC-09 | PASS | `test_session_id_with_space_rejected`, `test_principal_id_too_long_rejected` — two fields, two schemas |
| AC-10 | PASS | `test_profile_version_rejects_invalid_semver`/`_accepts_valid_semver`, `test_capability_version_rejects_invalid_semver`/`_accepts_valid_semver` — exact `01.0.0`/`1.01.0`/`1.0.01`/`1.0.0-alpha`/`1.0.0+build` rejected, `0.0.0`/`1.0.0` accepted |
| AC-11 | PASS | `test_structurally_malformed_timestamp_rejected`, `test_calendrically_impossible_month_rejected`, `_day_rejected`, `_hour_rejected` (2 schemas each), `test_valid_timestamps_still_accepted`; `FormatChecker` confirmed supplied in every validator (see AC-30) |
| AC-12 | PASS | `test_invalid_state_rejected`, `test_all_six_valid_states_accepted` |
| AC-13 | PASS | `test_zero_rejected`, `test_negative_rejected`, `test_non_integer_rejected`, `test_null_accepted` |
| AC-14 | PASS | `test_matching_fixtures_are_consistent`, `test_mismatched_profile_version_detected` — test-only helper `_fixtures_have_consistent_identity`, never shipped outside the test suite |
| AC-15 | PASS | `test_invalid_status_rejected`, `test_non_string_in_allowed_profiles_rejected` |
| AC-16 | PASS | `test_required_fields`, `test_status_enum_closed` (profile-manifest) |
| AC-17 | PASS | `test_required_fields`, `test_version_is_optimistic_concurrency_integer`, `test_ownership_closed_when_present` (operational-session) |
| AC-18 | PASS | `test_required_fields`, `test_risk_class_enum_closed` (command-envelope) |
| AC-19 | PASS | `test_required_fields`, `test_data_state_enum_closed`, `test_sequence_is_non_negative_integer`, `test_causation_id_nullable` (event-envelope) |
| AC-20 | PASS | `test_required_fields`, `test_status_enum_closed` (capability-manifest) |
| AC-21 | PASS | `test_command_envelope_top_level_unknown_rejected_payload_open`, `test_event_envelope_top_level_unknown_rejected_payload_open` |
| AC-22 | PASS | `test_no_provider_tokens_in_any_schema_text` — 8 tokens scanned across all six raw files, zero matches |
| AC-23 | PASS | `test_module_docstring_states_claim_boundary`, `test_schema_descriptions_state_contract_only` — every contract-owned schema's `description` field states "Contract-only" and "no runtime state machine" |
| AC-24 | PASS | `git diff --stat` against `docs/architecture/PLATFORM_BOUNDARY_AND_PORTING_RULES.md` and `docs/decisions/ADR_2026-07-23_GREENFIELD_PLATFORM_AND_PROFILE_PORTING.md` is empty; `contracts/core/*.schema.json` are JSON data files with no imports, so no forbidden dependency edge exists |
| AC-25 | PASS | Exactly one entry added to `docs/catalog/MODULE_REGISTRY.json`, byte-identical to the authorized entry (`id: contracts-core-f1a`, `status: CONTRACT_ONLY`), added only after all 177 tests passed |
| AC-26 | PASS | `docs/catalog/MODULE_CATALOG.md` regenerated only via `scripts/manage_cvf_downstream_catalog.ps1 -Write`; `docs/catalog/ARTIFACT_REGISTRY.json` untouched; `docs/INDEX.md` regenerated but byte-identical (empty `git diff`) |
| AC-27 | PASS | All 116 pre-existing tests still pass unmodified; 61 new F1A tests are additive |
| AC-28 | PASS | See "Repository gate commands and output" below |
| AC-29 | PASS | No AI/agent provider API was invoked by this BUILD or its tests; only public PyPI package-registry reads (no credentials) were performed to install the pinned validator |
| AC-30 | PASS | `test_date_time_checker_is_registered`, `test_every_validator_in_suite_has_format_checker` — every constructed validator in this suite passes a non-`None` `format_checker` |
| AC-31 | PASS | `test_arbitrary_nested_metadata_keys_accepted`, `test_arbitrary_nested_payload_keys_accepted`, `test_same_unknown_key_at_top_level_rejected`, `test_nested_state_key_does_not_satisfy_required_top_level_state`, `test_nested_version_key_does_not_satisfy_required_top_level_version` |

## Repository gate commands and output

```text
$ /path/to/isolated-venv/python -m unittest discover -s tests -p "test_*.py"
Ran 177 tests in 67.751s
OK
```

(116 pre-existing + 61 new F1A tests = 177; re-run again after the Module
Registry/Catalog change with the same result, 177/177 OK, `75.343s`.)

```text
$ git diff --check
(no output; exit 0 — clean)
```

```text
$ powershell -ExecutionPolicy Bypass -File scripts/manage_cvf_downstream_catalog.ps1 -Check
[PASS] Governed downstream catalog is valid and generated views match source truth.
```

```text
$ powershell -ExecutionPolicy Bypass -File <cvf-core>/scripts/check_cvf_workspace_agent_enforcement.ps1 -ProjectPath <this project>
RESULT: PASS (25/25 checks passed)
```

Post-BUILD pin re-verification: target HEAD = `origin/main` =
`d731762a9e135b075261831ed7eb0df4badc98dd` (unchanged — nothing has been
staged, committed, or pushed by this BUILD); CVF core HEAD =
`27137db4d9aa2aea931ddd2507185d5c24943080` (unchanged); both read-only
inputs unchanged.

## Known limitations / claim boundary

- **Contracts only.** No `OperationalSession` runtime state machine, no
  command execution, no governance enforcement, and no provider behavior is
  implemented or claimed. `contracts/core/*.schema.json` are pure JSON data
  files with zero imports; nothing under `contracts/` is executable.
- **No live provider/AI call was made or required.** This BUILD asserts
  schema behavior only.
- **Cross-document identity consistency is test-only, not runtime.** The
  `F1A-AC-14` helper `_fixtures_have_consistent_identity` lives solely in
  `tests/contracts/test_f1a_contracts.py`; it is not exported, imported
  elsewhere, or shipped as production code, per `ADR-OW-005` Decision 7's
  stated limitation.
- **`capability-invocation.schema.json` remains out of scope**, as does all
  `agent-operations/**`/`live-view/**` design input — deferred to F1D/F4/F5/F6
  per the roadmap, not silently dropped.
- **No F1B+ capability exists.** No `OperationalSession` runtime, profile
  registry, command handler, provider adapter, database schema/migration,
  API route, or frontend change was made.
- **The validator dependency is real and pinned, not fabricated.** All 19
  packages, exact versions and hashes were verified installable and
  hash-matching this round, in a disposable isolated environment; the
  requirements file materializes exactly `ADR-OW-005` Decision 12's table.

## Continuity

`IMPLEMENTATION_STATUS.json`, `CVF_SESSION/ACTIVE_SESSION_STATE.json`, and
`CVF_SESSION/handoffs/AGENT_HANDOFF_V1_2026-07-23.md` are updated in this
same BUILD to record this self-report. This is a **self-report by
IMPLEMENTATION_WORKER, not an independent review**. No REVIEW_PASS is
self-granted. No file was staged, committed, or pushed. No FREEZE occurred.

## Next governed move

Codex acts as independent REVIEWER over this BUILD evidence against
`OW-F1A-SPEC-001`'s `F1A-AC-01` through `F1A-AC-31`, including independently
reproducing the 19-package hash-locked install in a fresh isolated
environment, independently re-running the full 177-test suite, and
independently verifying the Module Registry entry and catalog regeneration.

## Independent review finding and bounded reviewer amendment

- `F1A-BR1 — FORMAT_EXTRA_NOT_MATERIALIZED`: independent review found that
  the lock contained `jsonschema==4.26.0` while the reviewed authorization
  consistently required `jsonschema[format-nongpl]==4.26.0`.
- The installed closure happened to work because every dependency of the
  extra was also pinned explicitly, but functional equivalence does not
  replace the exact authorized dependency expression.
- Bounded reviewer amendment: changed only that requirement expression to
  `jsonschema[format-nongpl]==4.26.0`, retaining the exact authorized
  version, wheel hash, other 18 package lines, platform policy and claim
  boundary. No schema, test, catalog entry or generated view changed.
- This amendment makes no new implementation choice; it materializes the
  exact expression already fixed by ADR-OW-005 and OW-F1A-WO-001. Its
  disposition remains pending the fresh isolated install, 177-test run and
  repository gates recorded by the independent reviewer.

Post-amendment independent verification:

- Fresh CPython 3.13.12/win_amd64 virtual environment: PASS.
- `pip install --require-hashes`: PASS with the corrected extra expression.
- `pip check`: `No broken requirements found`.
- `pip freeze`: exactly the authorized 19 package/version pairs.
- `date-time` checker registered; valid/invalid-month/invalid-day/
  invalid-hour outcomes were `[true, false, false, false]`.
- Full repository suite: 177/177 PASS.
- Golden catalog: PASS.
- Workspace doctor: PASS 25/25.
- JSON parsing and `git diff --check`: PASS.

Independent disposition: `F1A_BUILD_REVIEW_PASS`; `F1A-AC-01` through
`F1A-AC-31` PASS and `F1A-BR1` is closed without waiver. This receipt remains
the BUILD evidence artifact; the separate Codex-owned C3 independent review
receipt records the later FREEZE disposition.
