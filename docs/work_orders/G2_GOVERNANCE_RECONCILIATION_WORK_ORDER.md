# Work Order — G2 Governance Reconciliation

- Work order ID: `OW-G2-WO-001`
- Date: 2026-07-23 (repaired 2026-07-23, repair round 1)
- Status: REPAIRED — not yet authorized for BUILD (requires Codex independent
  re-review of this package first)
- Risk: R2 governance-significant
- Authorization: owner-directed governance reconciliation round, 2026-07-23
- Governing decision: `ADR-OW-003`
- Governing spec: `OW-G2-SPEC-001`

## Repair round 1 — findings and disposition (2026-07-23)

Independent Codex review of the originally-authored package returned four
findings. Full narrative and verification evidence is in `ADR-OW-003`'s
"Repair History" section and the active handoff's repair entry; disposition
of each is recorded here for the work order's own scope:

- **G2-R1 — BLOCKER_BASELINE_DRIFT (repaired).** Public CVF core `origin/main`
  advanced from this package's original target
  `571cb21b7026f0cd925279ba698bf30a291a4644` to
  `27137db4d9aa2aea931ddd2507185d5c24943080` while the package awaited review.
  The four Golden Kit payload files are byte-identical between the two
  commits (verified by `git diff` across both commits, zero diff). Repaired
  by running the official
  `scripts/update_cvf_workspace_public_core.ps1 -WorkspaceRoot
  "D:\UNG DUNG AI\TOOL AI 2026\CVF-Workspace"` reconciler (no
  `-UpdateProjectManifests`, `-OverlaySourcePath`, or
  `-AllowPendingCoreBackup`); prior hidden core preserved at
  `_cvf-core-backups/.Controlled-Vibe-Framework-CVF-20260723-200246`. Hidden
  core is now HEAD = origin/main = `27137db4d9aa2aea931ddd2507185d5c24943080`,
  worktree clean. All SHA references in `ADR-OW-003`, `OW-G2-SPEC-001`, and
  this work order are re-pinned to `27137db4d9aa2aea931ddd2507185d5c24943080`.
  The guard/doctor shipped at that commit is the authority for the
  independent re-review that follows this repair.
- **G2-R2 — CONTINUITY_PHASE_ROLE_DRIFT (repaired).**
  `CVF_SESSION/ACTIVE_SESSION_STATE.json` incorrectly still read
  `currentMode: FREEZE`, `activePhase: FREEZE`, `activeRole: ORCHESTRATOR`
  from the closed F0 tranche instead of reflecting this open G2 WORK_ORDER
  tranche. Corrected to `currentMode: WORK_ORDER`,
  `activePhase: WORK_ORDER`, `activeRole: WORK_ORDER_AUTHOR`.
- **G2-R3 — STALE_F0_CLAIM (repaired).** The active handoff's G2 entry stated
  "F0 is authorized but not yet built" in its closing claim boundary, which
  contradicts F0's actual, already-recorded status: F0 BUILD complete,
  independent REVIEW_PASS recorded, FREEZE complete, commits C1
  `8c193984c5fc158ca65ea554dd8d4934d12c28f4`, C2
  `39541d5e84b06f8650ce2b0f6341425c7a05d7bf`, C3
  `3064d4bce08d36f553516d59719358fd8788cbcf` committed. Module Registry
  remains empty and no runtime has been imported into the target — that part
  of the original claim was accurate and is preserved. Corrected in the
  active handoff.
- **G2-R4 — INCOMPLETE_ROLE_ROUTE (repaired).** The role route below omitted
  `IMPLEMENTATION_WORKER` and the post-BUILD independent re-review/repair
  loop, understating what full BUILD-through-FREEZE actually requires for R2
  work. Expanded below.

## Role route

```text
ORCHESTRATOR
-> SPEC_AUTHOR
-> WORK_ORDER_AUTHOR
-> REVIEWER                          (Codex, independent — never Claude)
-> COMMIT_STEWARD (C1 authorization) (Codex)
-> IMPLEMENTATION_WORKER             (BUILD, after REVIEW_PASS on this package)
-> REVIEWER                          (Codex, independent post-BUILD review)
-> REPAIR_WORKER if needed           (bounded repair of accepted findings)
-> RE_REVIEW                         (Codex, independent)
-> COMMIT_STEWARD (C2/C3)            (Codex)
-> CLOSER                            (Codex)
-> SESSION_SYNC_STEWARD              (continuity/catalog closure)
-> ORCHESTRATOR                      (parked, next tranche)
```

`ORCHESTRATOR -> SPEC_AUTHOR -> WORK_ORDER_AUTHOR` and any `REPAIR_WORKER`
pass on the authorization package itself are held by Claude (this round,
single agent, recorded role transitions). Every `REVIEWER`, `COMMIT_STEWARD`,
`RE_REVIEW`, `CLOSER`, and `SESSION_SYNC_STEWARD` step is Codex, independent
of Claude. Claude does not self-grant REVIEW_PASS, does not stage, does not
commit, and does not push at any point in this tranche.

## Authorization-round changed set (this round only — authoring, not BUILD)

Exactly these paths may be created or modified while authoring this package,
before Codex review:

```text
docs/decisions/ADR_2026-07-23_GOLDEN_DOWNSTREAM_CATALOG_RECONCILIATION.md
docs/specs/G2_GOVERNANCE_RECONCILIATION_SPEC.md
docs/work_orders/G2_GOVERNANCE_RECONCILIATION_WORK_ORDER.md
docs/catalog/ARTIFACT_REGISTRY.json   (register these 3 artifacts, OLD schema — pre-migration)
docs/INDEX.md                          (regenerated by scripts/manage_catalog.py --write, OLD schema)
IMPLEMENTATION_STATUS.json             (record OW-G2-WO-001 as authored/open, not yet authorized)
CVF_SESSION/ACTIVE_SESSION_STATE.json  (nextAllowedMove reflects pending G2 review)
CVF_SESSION/handoffs/AGENT_HANDOFF_V1_2026-07-23.md (acknowledgment entry)
```

No BUILD file (`.cvf/manifest.json`, any `scripts/manage_cvf_downstream_catalog.ps1`,
schema, or registry migration) is touched during this authorization round.
Registering these 3 documents uses the **existing, not-yet-migrated** schema —
migrating the registry itself is BUILD work under the ceiling below, not
authoring work.

## BUILD changed-set ceiling (authorized only after Codex REVIEW_PASS on this package)

This is a ceiling, not a commitment to touch every path. BUILD may create,
modify, or (only where explicitly marked) delete up to:

```text
.cvf/manifest.json
AGENTS.md
docs/CVF_BOOTSTRAP_LOG_20260723.md
scripts/manage_cvf_downstream_catalog.ps1
scripts/lib/downstream_catalog/CvfDownstreamCatalogLib.ps1
docs/catalog/schemas/ARTIFACT_REGISTRY.schema.json
docs/catalog/schemas/MODULE_REGISTRY.schema.json
docs/catalog/ARTIFACT_REGISTRY.json
docs/catalog/MODULE_REGISTRY.json
docs/catalog/README.md
docs/INDEX.md
docs/catalog/MODULE_CATALOG.md
scripts/manage_catalog.py
tests/test_catalog_management.py
docs/catalog/ARTIFACT_REGISTRY.schema.json    (old path — retire per ADR-OW-003 #6)
docs/catalog/MODULE_REGISTRY.schema.json      (old path — retire per ADR-OW-003 #8)
docs/reviews/G2_*
IMPLEMENTATION_STATUS.json
CVF_SESSION/ACTIVE_SESSION_STATE.json
CVF_SESSION/handoffs/**
docs/decisions/ADR_2026-07-23_GOLDEN_DOWNSTREAM_CATALOG_RECONCILIATION.md
docs/specs/G2_GOVERNANCE_RECONCILIATION_SPEC.md
docs/work_orders/G2_GOVERNANCE_RECONCILIATION_WORK_ORDER.md
```

Deletion is permitted **only** for the legacy catalog writer/schema files
explicitly listed above (`scripts/manage_catalog.py`,
`tests/test_catalog_management.py`, the two old-path schema files), and only
after BUILD records the regression evidence required by `OW-G2-SPEC-001`
G2-AC-11. No other path in this ceiling may be deleted. No path outside this
ceiling may be created, modified, or deleted.

## Explicit exclusions (stop and escalate if touched)

```text
docs/roadmaps/**
apps/**
packages/**
database/**
provenance/shift-operations/**
scripts/source_intake/**
tests/source_intake/**
.github/**
```

Also excluded: the source repository `shift-operations-workspace`,
`operations-workspace-all-phases`, the CVF core repository (read-only
reference only), and any API-key/secret file.

## Roles and ownership

- **ORCHESTRATOR / SPEC_AUTHOR / WORK_ORDER_AUTHOR** (Claude, this round):
  authors `ADR-OW-003`, `OW-G2-SPEC-001`, `OW-G2-WO-001`, and the 5
  authorization-round continuity/catalog registration files. Does not touch
  the BUILD ceiling. Does not commit.
- **REVIEWER** (Codex, independent from Claude — mandatory for R2): reviews
  this package for completeness against the 22 acceptance criteria, the
  28-row disposition table, and the stop conditions below, before any BUILD
  work begins.
- **IMPLEMENTATION_WORKER** (assigned at BUILD authorization, after
  REVIEW_PASS on this package — role not yet opened by this work order):
  implements only the BUILD ceiling.
- **COMMIT_STEWARD** (Codex, exclusively): stages, commits, pushes; rehearses
  each commit in a temporary sibling worktree before it is considered final.
- **CLOSER** (Codex): confirms FREEZE disposition and continuity closure.

## Tasks (this authorization round)

1. Verify baseline guards (branch, HEAD, origin, worktree, core pin) — done,
   recorded above.
2. Author `ADR-OW-003` with the full 28-entry disposition table.
3. Author `OW-G2-SPEC-001` with G2-AC-01 through G2-AC-22.
4. Author this work order with ceiling, roles, commit plan, stop conditions.
5. Register the 3 new artifacts in `docs/catalog/ARTIFACT_REGISTRY.json`
   (existing schema), regenerate `docs/INDEX.md` via
   `python scripts/manage_catalog.py --write`.
6. Update `IMPLEMENTATION_STATUS.json` (`activeWorkOrders` includes
   `OW-G2-WO-001`, status AUTHORED-NOT-AUTHORIZED), `ACTIVE_SESSION_STATE.json`
   (`nextAllowedMove`: await Codex independent review of the G2 package), and
   the active handoff (acknowledgment entry).
7. Stop. Hand off to Codex as REVIEWER. Do not proceed to BUILD.

## Commit plan (owned exclusively by Codex COMMIT_STEWARD)

- **C1** — authorization artifacts (`ADR-OW-003`, `OW-G2-SPEC-001`,
  `OW-G2-WO-001`) + authorization continuity/catalog registration
  (`docs/catalog/ARTIFACT_REGISTRY.json`, `docs/INDEX.md`,
  `IMPLEMENTATION_STATUS.json`, `CVF_SESSION/ACTIVE_SESSION_STATE.json`,
  `CVF_SESSION/handoffs/**`).
- **C2** — core pin update, Golden Kit migration (schemas, manager, library,
  both registries migrated, `docs/catalog/README.md` rewritten,
  `docs/INDEX.md`/`docs/catalog/MODULE_CATALOG.md` regenerated by the Golden
  manager), legacy writer retirement, tests, `docs/CVF_BOOTSTRAP_LOG_20260723.md`
  and `AGENTS.md` reconciliation.
- **C3** — independent review receipt (`docs/reviews/G2_*`) + continuity/
  FREEZE closure (`IMPLEMENTATION_STATUS.json`, `CVF_SESSION/**`).

Each commit is created by Codex COMMIT_STEWARD, then rehearsed post-commit
and pre-push in a temporary sibling worktree. A commit is not pushed or
treated as accepted until its rehearsal passes (G2-AC-22).
No `git add .` / `git add -A`. No amend, rebase, or force-push.

## Stop conditions

Stop immediately and report the named blocked-state if any of the following
occurs:

- baseline HEAD/origin drift from the values recorded at INTAKE;
- CVF core SHA/remote drift from `27137db4d9aa2aea931ddd2507185d5c24943080`
  (re-pinned per `G2-R1`; if the public tip advances again during BUILD, stop
  and report drift — do not silently choose a new SHA) / the public remote;
- worktree not clean at any checkpoint;
- migration would drop an artifact path with no disposition in `ADR-OW-003`;
- the Golden schema cannot represent a claim without misstating the claim
  boundary;
- `docs/catalog/MODULE_REGISTRY.json` gains a module entry;
- the competing-writer question (`scripts/manage_catalog.py` vs the Golden
  manager) is left unresolved at FREEZE;
- a generated view requires hand-editing to pass `-Check`;
- any F0 provenance/runtime path changes;
- the workspace doctor fails for a reason outside this tranche's declared
  scope;
- CVF core requires modification;
- the changed-set ceiling needs to expand beyond what is listed above;
- a secret is encountered;
- a live provider call becomes necessary;
- rollback or test strategy cannot be determined.

On any stop condition: halt, do not commit, and report
`BLOCKED_BASELINE_DRIFT` or the specific named condition — do not choose
silently among alternatives.

## Completion boundary

This work order authorizes authorship of the G2 package and a bounded BUILD
ceiling for a *future*, separately reviewed BUILD phase. It does not itself
authorize BUILD to begin — BUILD begins only after Codex records an
independent REVIEW_PASS on this package. It does not touch the roadmap, does
not import or implement any runtime module, and does not create any new CVF
control or capability claim.
